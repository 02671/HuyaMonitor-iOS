# 虎牙监控

Windows x64 便携小工具：输入虎牙房号，看实时弹幕、听直播音频。

详细步骤见同目录 `使用说明.txt`。双击 `HuyaDanmu.bat` 启动。

## iOS 版

`ios/` 目录是在原 Python 工具基础上移植的 iOS 原生应用（SwiftUI + AVPlayer），功能一致，支持锁屏和后台继续播放音频，自用签名安装。构建与安装步骤见 `ios/README-iOS.md`。
